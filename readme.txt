Skapa en databas med hjälp av SQL-kommando:
CREATE DATABASE gik2xkProjekt

Kör npm install

Globala npm-paket:
Installera sequelize-cli: npm install -g sequelize-cli
Installera Nodemon: npm install -g nodemon
Installera Express-generator: npm install -g express-generator